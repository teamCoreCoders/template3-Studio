import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import { ThemeProvider } from '@/components/theme-provider';
import { Header } from '@/components/header';
import { Footer } from '@/components/footer';

const inter = Inter({ 
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter',
});

export const metadata: Metadata = {
  title: 'Portfolio - Exceptional Digital Work',
  description: 'Showcasing exceptional digital work — crafted with precision, passion, and purpose.',
  keywords: 'portfolio, web development, ui/ux design, digital agency, modern design',
  authors: [{ name: 'Portfolio Team' }],
  openGraph: {
    title: 'Portfolio - Exceptional Digital Work',
    description: 'Showcasing exceptional digital work — crafted with precision, passion, and purpose.',
    type: 'website',
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning className={inter.variable}>
      <body className={inter.className}>
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange={false}
        >
          <Header />
          <main className="min-h-screen">
            {children}
          </main>
          <Footer />
        </ThemeProvider>
      </body>
    </html>
  );
}